const Reducer = (state, action)=>{
    switch (action.type){
        case "LOGIN START":
            return{
                user: null,
                isFetching: true,
                error: false,
            };
        case "LOGIN START":
            return{
                user: null,
                isFetching: true,
                error: false,
            };
        case "LOGIN START":
            return{
                user: null,
                isFetching: true,
                error: false,
            };
        default:
            return state;
    }
}